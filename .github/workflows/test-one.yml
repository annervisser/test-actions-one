name: test run one
on: 
  - push
  - release
jobs:
  actual-job:
    runs-on: ubuntu-latest
    steps:
      - name: Set Github environment variables
        uses: ScientaNL/github-actions-env-toolkit@main
           
